{
  "name": "nexus-agent",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:server": "pnpm --filter server dev",
    "dev:ui": "pnpm --filter ui dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "dependencies": {
    "@imdanibytes/nexus-ui": "link:../../app/packages/nexus-ui"
  }
}
